using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TurretDronePropeller : MonoBehaviour
{   
    
    void Start()
    {
        // Move in direction, move up as much as gravity regularly, but if lower than a certain point increase the force with 1. 
        // Turn right or left like 45 degrees depending where the detected player is, and hover above if the player is within 5f on x.
        // Follow the player 5 seconds after detection stopped.
    }


    void Update()
    {
      
    }
}
